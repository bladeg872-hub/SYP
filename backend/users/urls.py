from django.urls import path

from .views import (
    AdminAccountsView,
    AdminCreateUserView,
    AdminUpdateUserProfileView,
    AnalyticsSummaryView,
    AuditSummaryView,
    ChallanSlipGenerateView,
    ChangePasswordView,
    DeclineUserView,
    DashboardSummaryView,
    ExpenseEntryDetailView,
    ExpenseEntryListCreateView,
    LoginView,
    ManagerDeleteUserView,
    ManagerTeamMembersView,
    ProfileView,
    PurchaseEntryDetailView,
    PurchaseEntryListCreateView,
    RefreshView,
    RegisterView,
    ReportsSummaryView,
    SalesEntryDetailView,
    SalesEntryListCreateView,
    PendingUsersView,
    TdsCertificateGenerateView,
    VerifyUserView,
)

urlpatterns = [
    path("register/", RegisterView.as_view(), name="register"),
    path("admin/create-user/", AdminCreateUserView.as_view(), name="admin-create-user"),
    path("admin/accounts/", AdminAccountsView.as_view(), name="admin-accounts"),
    path("admin/team-members/", ManagerTeamMembersView.as_view(), name="team-members"),
    path("admin/pending-users/", PendingUsersView.as_view(), name="admin-pending-users"),
    path("admin/verify-user/", VerifyUserView.as_view(), name="admin-verify-user"),
    path("admin/decline-user/", DeclineUserView.as_view(), name="admin-decline-user"),
    path("admin/delete-user/", ManagerDeleteUserView.as_view(), name="manager-delete-user"),
    path("admin/update-user/", AdminUpdateUserProfileView.as_view(), name="admin-update-user"),
    path("login/", LoginView.as_view(), name="login"),
    path("refresh/", RefreshView.as_view(), name="refresh"),
    path("me/", ProfileView.as_view(), name="me"),
    path("change-password/", ChangePasswordView.as_view(), name="change-password"),
    path("sales/", SalesEntryListCreateView.as_view(), name="sales-list-create"),
    path("sales/<int:id>/", SalesEntryDetailView.as_view(), name="sales-detail"),
    path("purchases/", PurchaseEntryListCreateView.as_view(), name="purchases-list-create"),
    path("purchases/<int:id>/", PurchaseEntryDetailView.as_view(), name="purchases-detail"),
    path("expenses/", ExpenseEntryListCreateView.as_view(), name="expenses-list-create"),
    path("expenses/<int:id>/", ExpenseEntryDetailView.as_view(), name="expenses-detail"),
    path("dashboard/", DashboardSummaryView.as_view(), name="dashboard-summary"),
    path("reports/", ReportsSummaryView.as_view(), name="reports-summary"),
    path("reports/tds-certificate/", TdsCertificateGenerateView.as_view(), name="tds-certificate"),
    path("reports/challan-slip/", ChallanSlipGenerateView.as_view(), name="challan-slip"),
    path("analytics/", AnalyticsSummaryView.as_view(), name="analytics-summary"),
    path("audit/", AuditSummaryView.as_view(), name="audit-summary"),
]
